services:
  mongo:
    container_name: mongo
    image: mongo:latest
    environment:
      - MONGO_DATA_DIR=/data/db
    ports:
      - "27018:27017"
    volumes:
      - mongo:/data/db
  
volumes:
  mongo: